# Master runtime configuration.
# Without this file the master picks up config.properties from the worker jar, which is a
# developer's local file and has no storage.file.path - uploads then land in a folder called
# "null". Override the folder with -Dstorage.file.path or by POSTing /configuration/configapp.
storage.file.path=${java.io.tmpdir}/jneuropallium-master
