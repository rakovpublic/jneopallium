<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.rakovpublic.jneopallium</groupId>
        <artifactId>wrapper</artifactId>
        <version>1.0</version>
    </parent>
    <artifactId>worker</artifactId>
    <version>1.0-SNAPSHOT</version>

    <!--
      TRANSITIONAL module (see docs/refactor/worker-modularization-plan.md).
      The lightweight engine + contracts now live in worker-core. This module
      still aggregates the protocol bridges, the per-domain neuron/signal/
      processor implementations, the `ai` AGI package, and the demos until each
      is carved into its own artifact in later phases. It depends on
      worker-core and keeps only the bridge/domain-specific third-party
      libraries (milo, mqtt/tahu, mavlink, opentelemetry). The shared JSON /
      logging / kafka libraries used directly by bridge and domain code are
      declared explicitly here rather than relied on transitively.
    -->

    <dependencies>
        <!-- The lightweight core engine + contracts. -->
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>worker-core</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <!-- Shared neuromodulatory foundation (used by the domain impls and the
             higher-level ai/* neurons still held here). -->
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>agi-base</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>

        <!-- Extracted leaf-domain implementation modules (Phase 2, batch A).
             The transitional worker still holds the bridges, demos, and the
             higher-level ai/* neurons that reference these domain classes, so
             it depends on each extracted domain jar. -->
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-adfraud</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-affect</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-bci</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-clinical</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-curiosity</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-embodiment</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-glia</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-industrial</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-agi</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-llm</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-security</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-sleep</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-swarm</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.rakovpublic.jneopallium</groupId>
            <artifactId>domain-tutoring</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>

        <!-- Core JSON (used directly by bridge/domain code) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>${log4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>${log4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
            <version>${log4j.version}</version>
        </dependency>

        <!-- Annotations (jakarta replaces javax on Java 17+) -->
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>${jakarta-annotation.version}</version>
        </dependency>

        <!-- Kafka (used directly by the Kafka bridge transport) -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>${kafka.version}</version>
        </dependency>

        <!-- ============== OpenTelemetry (Bridge 09) ==============
             Versions are pulled from the opentelemetry-bom imported in the
             parent. Export-only - no input/aggregator surface. -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk-extension-autoconfigure</artifactId>
        </dependency>
        <!-- In-memory exporters used by the unit tests for traces/metrics/logs. -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk-testing</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ============== MQTT + Sparkplug B (Bridge 02) ============== -->
        <dependency>
            <groupId>com.hivemq</groupId>
            <artifactId>hivemq-mqtt-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.tahu</groupId>
            <artifactId>tahu-core</artifactId>
        </dependency>

        <!-- ============== MAVLink (Bridge 12) ============== -->
        <dependency>
            <groupId>io.dronefleet.mavlink</groupId>
            <artifactId>mavlink</artifactId>
        </dependency>

        <!-- ============== OPC UA (Phase 2) ============== -->
        <dependency>
            <groupId>org.eclipse.milo</groupId>
            <artifactId>milo-sdk-client</artifactId>
        </dependency>
        <!-- Server SDK for the embedded OPC UA test harness. -->
        <dependency>
            <groupId>org.eclipse.milo</groupId>
            <artifactId>milo-sdk-server</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>-Xms16m -Xmx256m -XX:+UseSerialGC -XX:MaxMetaspaceSize=192m -XX:ReservedCodeCacheSize=64m</argLine>
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
