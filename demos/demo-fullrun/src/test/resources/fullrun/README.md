# Full-Run Demo Resources

The full-run contexts, layer metadata, model JARs, outputs, audits, manifests, and summary are generated under `target/jneopallium-fullrun-demos`.

These resource folders document each deterministic scenario used by the generated demos. They intentionally do not contain absolute-path context JSON because the launcher writes platform-specific paths at run time.
